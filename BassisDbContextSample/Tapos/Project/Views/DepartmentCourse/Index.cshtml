@model IEnumerable<Project.Models.DepartmentCourse>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CourseID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CourseID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepartmentCourseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepartmentCourseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepartmentCourseID })
        </td>
    </tr>
}

</table>
